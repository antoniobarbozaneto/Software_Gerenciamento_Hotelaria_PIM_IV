--BD Postegrsql
CREATE TABLE tbl_Pagamento(
NFE INT NOT NULL GENERATED BY DEFAULT AS IDENTITY, --Auto Incremento
Num_Reserva INT NOT NULL,
Forma_Pagamento VARCHAR(255) NOT NULL,
Num_Parcela NUMERIC,
Valor_Total NUMERIC NOT NULL,
Valor_Pago NUMERIC NOT NULL,
Valor_Parcela NUMERIC NOT NULL,
Troco NUMERIC,
Dt_Pagamento VARCHAR(255) NOT NULL,
Autor INT NOT NULL
);

ALTER TABLE tbl_Pagamento ADD CONSTRAINT PK_Pagamento PRIMARY KEY (NFE);
ALTER TABLE tbl_Pagamento ADD CONSTRAINT FK_Pagamento FOREIGN KEY (Num_Reserva) REFERENCES tbl_Reserva (Num_Reserva);
ALTER TABLE tbl_Pagamento ADD CONSTRAINT FK_Usuario FOREIGN KEY (Autor) REFERENCES tbl_Usuario(id_usuario);